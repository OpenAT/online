Updating Swagger UI

Locally install the lastest swagger-ui-dist package
via npm. Copy the swagger-ui-dist folder into

openapi_swagger_ui/static/src

Verify, that the controller modifies the index.html
document correctly.
